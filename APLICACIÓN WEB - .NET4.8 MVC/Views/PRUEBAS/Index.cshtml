@model IEnumerable<Colegio_Capitaria.Models.PRUEBAS>

@{
    ViewBag.Title = "Pruebas";
}

<h2>Pruebas</h2>

<p>
    @Html.ActionLink("Crear nueva", "Create")
</p>
<table class="table">
    <tr>
        <th>
            CURSO
        </th>
        <th>
            ASUNTO DE PRUEBA
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CURSOS.DESCRIPCION)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DESCRIPCION)
        </td>
        <td>
            @Html.ActionLink("Editar", "Edit", new { id=item.ID_PRUEBA }) |
            @Html.ActionLink("Detalle", "Details", new { id=item.ID_PRUEBA }) |
            @Html.ActionLink("Eliminar", "Delete", new { id=item.ID_PRUEBA })
        </td>
    </tr>
}

</table>
